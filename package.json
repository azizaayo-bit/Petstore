{
  "name": "petstore-playwright-ai-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "test:csv": "node run_petstore_tests.mjs",
    "test:petstore-plan": "node run_petstore_plan_tests.mjs",
    "test:petstore-all": "node run_all_tc.mjs",
    "autopush": "git add . && git commit -m \"auto\" && git pull origin main --rebase && git push origin main"
  },
  "dependencies": {
    "csv-parse": "^5.0.0",
    "node-fetch": "^3.4.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.39.0"
  },
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/azizaayo-bit/Petstore.git"
  },
  "license": "MIT"
}
